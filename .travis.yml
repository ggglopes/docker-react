language: generic
sudo: required
services:
    - docker

before_install:
    - docker build -t gbelinelo/docker-react -f Dockerfile.dev .

script:
    - docker run -e CI=true gbelinelo/docker-react npm run test